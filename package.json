{
  "name": "rio-batch-merge",
  "version": "1.0.0",
  "description": "CAP application for merging batch splits on Outbound Delivery Items",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^9.9.2",
    "jest": "^30.4.2"
  },
  "scripts": {
    "start": "cds run",
    "test": "jest --testPathPattern=test/"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
